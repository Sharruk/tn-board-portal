[project]
name = "tn-board-platform"
version = "1.0.0"
description = "TN State Board Learning Platform — FastAPI backend"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.9",
    "python-dotenv>=1.0.1",
    "sqlalchemy>=2.0.35",
    "werkzeug>=3.1.0",
    "gunicorn>=23.0.0",
]
