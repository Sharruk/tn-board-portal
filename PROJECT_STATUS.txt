====================================================
Project Name:
TN State Board Learning Platform

Version:
v1.0 MVP

Current Phase:
PHASE 3 COMPLETE — Awaiting approval to start Phase 4

Current Progress:
All three phases complete. Preview working.
  Phase 1 — FastAPI backend (15 endpoints, JWT, file upload)
  Phase 2 — React public frontend (7 pages, Axios service layer)
  Phase 3 — React admin panel (login, dashboard, papers management)
  Fix     — Vite preview host config (allowedHosts: true)

Last Updated:
2026-06-20
====================================================

PROGRESS SUMMARY

Phase 1 — FastAPI Backend:       [x] COMPLETE
Phase 2 — React Public Frontend: [x] COMPLETE
Phase 3 — Admin Panel:           [x] COMPLETE
Vite Preview Fix:                [x] COMPLETE
Phase 4 — Deployment:            [ ] PENDING (awaiting approval)

====================================================

WORKFLOW CONFIGURATION

Name: "FastAPI Backend"
Command: cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
Port: 8000 (console)
Status: Running ✓

Name: "Start application"
Command: cd frontend && npm run dev
Port: 5000 (webview — Replit preview)
Status: Running ✓

Vite config:
  host: '0.0.0.0'
  port: 5000
  allowedHosts: true        ← FIXED (was 'all' — broken in Vite 5)
  strictPort: true
  proxy /api → localhost:8000
  proxy /uploads → localhost:8000

====================================================

DATABASE STATUS

Tables:
* classes  ✓ — 4 rows (Class 9, 10, 11, 12)
* subjects ✓ — 32 rows
* papers   ✓ — 1 row (test paper)
* admins   ✓ — 1 row (admin / admin123)

====================================================

BACKEND ENDPOINTS (all working)

GET  /api/v1/classes                ✓
GET  /api/v1/classes/{id}           ✓
GET  /api/v1/classes/{id}/subjects  ✓
GET  /api/v1/subjects/{id}          ✓
GET  /api/v1/subjects/{id}/papers   ✓
GET  /api/v1/papers/recent          ✓
GET  /api/v1/papers/{id}            ✓
GET  /api/v1/exam-types             ✓
GET  /api/v1/search                 ✓
POST /api/v1/auth/login             ✓
GET  /api/v1/admin/papers           ✓ (JWT)
POST /api/v1/admin/papers           ✓ (JWT + multipart)
PUT  /api/v1/admin/papers/{id}      ✓ (JWT)
DELETE /api/v1/admin/papers/{id}    ✓ (JWT)

====================================================

FRONTEND PAGES (all working in preview)

Public:
  /                 ✓ Homepage
  /class/:id        ✓ Class subjects
  /subject/:id      ✓ Exam type cards
  /papers           ✓ Paper list + filter
  /paper/:id        ✓ Paper detail + download + YouTube
  /search           ✓ Search with filters
  /*                ✓ 404 page

Admin:
  /admin/login      ✓ Login form
  /admin/dashboard  ✓ Stats + recent papers (protected)
  /admin/papers     ✓ Full management: upload/edit/delete (protected)
  /admin            ✓ Redirects to /admin/dashboard

====================================================

ADMIN CREDENTIALS

Username: admin
Password: admin123
URL: [preview-url]/admin/login

====================================================

NEXT TASKS (Phase 4 — Deployment)

1. Set SESSION_SECRET env var
2. Connect Supabase PostgreSQL (swap DATABASE_URL)
3. Connect Supabase Storage for PDF uploads
4. npm run build → serve from FastAPI OR deploy Vercel + Railway
5. Lock down CORS to production domain
6. Change admin password to something strong

====================================================
