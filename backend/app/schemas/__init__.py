"""
Schemas package — Pydantic request/response models.

Schemas define the shape of data entering and leaving the API.
They are separate from ORM models (app/models/) and from
service-layer data classes.
"""
