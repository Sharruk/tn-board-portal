"""
Dependencies package — FastAPI dependency injection.

Reusable FastAPI dependencies (Depends()) live here.
Planned for future sprints:
  - get_current_user() — Firebase/JWT authentication
  - get_db() — database session per request
  - require_admin() — admin role guard

Sprint 02: Placeholder only.
"""
